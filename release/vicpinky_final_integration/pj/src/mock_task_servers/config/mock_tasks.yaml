servers:
  /nav/go_to:
    delay_sec: 3.0
    success: true
    phases:
      - planning
      - moving
      - arrived

  /dock/align:
    delay_sec: 3.0
    success: true
    phases:
      - searching_marker
      - aligning
      - aligned

  /arm/pick:
    delay_sec: 4.0
    success: true
    phases:
      - pre_grasp
      - grasp
      - lift

  /arm/place:
    delay_sec: 4.0
    success: true
    phases:
      - pre_place
      - place
      - release

  /arm/execute:
    delay_sec: 1.0
    success: true
    phases:
      - start
      - execute
      - complete

  /arm/press_button:
    delay_sec: 3.0
    success: true
    phases:
      - move_to_button
      - press
      - retreat

  /arm/homing:
    delay_sec: 3.0
    success: true
    phases:
      - prepare
      - homing
      - ready

  /elevator/wait_door_open:
    delay_sec: 5.0
    success: true
    phases:
      - waiting
      - checking_door
      - door_open

  /elevator/board:
    delay_sec: 5.0
    success: true
    phases:
      - wait_board_marker
      - boarding
      - boarded

  /elevator/exit:
    delay_sec: 5.0
    success: true
    phases:
      - wait_floor_marker
      - exiting
      - exited

  /floor/check:
    delay_sec: 2.0
    success: true
    phases:
      - detect_marker
      - verify_floor
      - floor_confirmed

  /map/switch:
    delay_sec: 2.0
    success: true
    phases:
      - load_map
      - set_initial_pose
      - map_ready

  /base/drive_straight:
    delay_sec: 1.0
    success: true
    phases:
      - wait_delay
      - driving
      - stopped

  /base/rotate:
    delay_sec: 1.0
    success: true
    phases:
      - rotating
      - stopped
